package model.geom

class Point2d {
	public var x : double
	public var y : double
	
	public new() {
		x = 0
		y = 0
	}
	
	public new(x : double, y : double) {
		this.x = x
		this.y = y
	}
	
	public def distance(p : Point2d) {
		return Math.sqrt(Math.pow(x - p.x, 2) + Math.pow(y - p.y, 2))
	}
}